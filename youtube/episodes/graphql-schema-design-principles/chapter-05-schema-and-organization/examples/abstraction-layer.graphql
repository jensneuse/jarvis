# Bad: Schema mirrors the REST API
type Query {
  # These are just REST endpoints with GraphQL syntax
  getApiV2UserProfile(userId: String!): UserProfileResponse
  postApiV2UserUpdate(body: UserUpdateBody!): UserUpdateResponse
}

# Good: Schema describes client capabilities
type Query {
  viewer: Viewer!
  user(id: ID!): UserProfile
}

type Mutation {
  updateProfile(input: UpdateProfileInput!): UpdateProfileResult!
}

# The backend can be REST, gRPC, a database, a microservice —
# the client doesn't know and doesn't care.
# You can migrate your entire backend without changing the schema.
